<div id='step'>
  <h2><%= @step.name %></h2>
  <%= link_to "Edit Step", [:edit, @goal, @step] %>
  <%= link_to "Delete Step", [@goal, @step], :method => :delete,
           :confirm => "Are you sure you want to delete this step?" %>
</div>
