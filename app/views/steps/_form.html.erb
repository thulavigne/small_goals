<%= form_for [@goal, @step] do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label :name %><br>
        </programlisting>
      </informalexample>
    <%= f.text_field :name %>
  </p>
  <%= f.submit %>
<% end %>
